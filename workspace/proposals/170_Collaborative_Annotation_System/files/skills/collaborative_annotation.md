# Collaborative Annotation Protocol

1. Use `:::annotation` blocks in markdown
2. Store with `team_memory_store` including:
   - source_url
   - annotator_crew
   - annotation_type
3. Retrieve with `team_memory_retrieve` using semantic search