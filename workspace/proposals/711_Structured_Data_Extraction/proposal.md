# Proposal #711: Structured Data Extraction

**Type:** skill  
**Created:** 2026-04-20T23:07:23.418139+00:00  

## Why this is useful

Problem: Agents struggle to reliably extract structured data (tables, lists, key-value pairs, entities) from unstructured text scraped from web pages or documents. Current approach is ad-hoc and error-prone. Solution: A skill teaching systematic approaches to data extraction using regex patterns, LLM-assisted extraction, and library-based parsers for common formats (emails, phones, dates, addresses, prices).

## What will change

- Modifies `skills/structured_data_extraction.md`

## Potential risks to other subsystems

- Uncategorised (skills): impact scope unclear

## Files touched

- `skills/structured_data_extraction.md`

## Original description

Problem: Agents struggle to reliably extract structured data (tables, lists, key-value pairs, entities) from unstructured text scraped from web pages or documents. Current approach is ad-hoc and error-prone. Solution: A skill teaching systematic approaches to data extraction using regex patterns, LLM-assisted extraction, and library-based parsers for common formats (emails, phones, dates, addresses, prices).

---

**To decide:** react 👍 to the Signal notification to approve, or 👎 to reject.  
Or reply `approve 711` / `reject 711` via Signal.
