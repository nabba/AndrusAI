# Closed initiatives archive (2026 Q1–Q2)

This file holds the full text of the closed-Q sections that previously lived in `CLAUDE.md` between lines 97 and 115 (commit at 2026-05-24 compaction pass, Gap #8).

The compaction was a deliberate one-shot move: the project crossed 200 KB of CLAUDE.md and the per-Q-section detail was making the doc too large to ingest in working memory. The contents below are preserved verbatim — every file path, master switch, ledger event kind, and behavior described still holds in the live system.

When you need the full design rationale for any of:

  * Q14 year-2+ risk register (PROGRAM §49 — drift digest, influence graph, goodhart recipe-selection divergence, embedding fingerprint drift, interest ossification, lock contention)
  * Q13 year-2+ resilience (PROGRAM §48 — migration drill, dependency_radar, tz_drift monitor)
  * Q12 self-understanding & philosophy (PROGRAM §47 — sentience-probe self-design)
  * Q11 creative synthesis (PROGRAM §46.18–§46.24 — analogy populator, concept-blend, novelty wrap, aesthetic score, idea evolution)
  * Q10 tech-radar → adoption loop (PROGRAM §46.13–§46.17 — trial runner, paper code-from-paper, multi-source feeds)
  * Q9 companion deepening (PROGRAM §46.6–§46.11 — travel, inbox handlers, action handlers, long-term goal review)
  * Q8 difficult-question solving (PROGRAM §46 — threads completion, approaches-tried distillation, workflow_template primitive)
  * Q7 self-evolution closure (PROGRAM §45 — architecture-request adoption, schema-aware coding sessions, recipe selection-rate trigger, inline ShinkaEvolve audit)
  * Q6 — Resilience drills + posture decision (PROGRAM §44 — 9 drills, Q6.4 audit follow-ups, Q6.5 closure)
  * Q5 — Targeted sentience experiments (PROGRAM §43 — AE-2, HOT-1, HOT-4, RPT-1)
  * Q4 Companion depth (PROGRAM §41 — tensions store, cross-modal patterns, surface arbitration)
  * Q3 / Q3.1 / Q3.2 / Q3.3 multi-year hygiene cleanup passes (PROGRAM §40)
  * Person-correlation stack + follow-ups (PROGRAM §42)

… read on. Each bullet below is one of those Q sections in its original, unedited form.

---

- Q14 year-2+ risk register (2026-05-16; PROGRAM.md §49 — 6 monitor-class items): operator's §10 audit named 6 emergent failure patterns. Audit: 1 already shipped, 2 partial, 3 missing. All 6 shipped this Q. **§49.1 Q14.1 identity drift digest** (`app/identity/drift_digest.py` + monthly healing monitor): reads `summarise_drift(30/90/365)` from continuity_ledger; computes aggregate acceleration = 30d / (365d/12); alerts ≥2.0 OR per-kind ≥3× annual baseline; daily-briefing snippet via `briefing_section()`; emits `identity_drift_acceleration` ledger landmark. **§49.2 Q14.2 influence graph + meta-agent loop drift**: new `app/healing/influence_graph/` package — `edges.py` curates ~50 `InfluenceEdge(producer, consumer, signal, kind)` tuples covering 30+ idle jobs / KBs / decision surfaces; `cycles.py` Tarjan's iterative SCC finds every closed loop. Pinning test asserts the named meta-agent loop IS detected. New `app/healing/monitors/feedback_loop_drift.py` watches the concrete loop: weekly Gini coefficient of `meta_agent_recipes.uses`; monotonic-increase ≥0.10 over 4 weekly samples → alert + `feedback_loop_drift` ledger landmark. **§49.3 Q14.3 goodhart_guard recipe-selection divergence**: extension to `app/goodhart_guard.py`. New `_detect_recipe_selection_divergence` signal type: top-10%-selected recipes with ≥20 outcomes AND `thumbs_up_rate < 30%` emit `GamingSignal` flowing through existing `goodhart_advisory_report` — no new aggregator. Severity scales (HIGH <0.15, MEDIUM <0.25, LOW <0.30). **§49.4 Q14.4 embedding-model fingerprint drift** (`app/healing/monitors/embedding_drift.py`): 20 anchor queries baselined on first run at `workspace/healing/embedding_anchors.json`; weekly re-embed; ANY anchor's self-similarity < 0.95 → alert + `embedding_model_swap` landmark. Catches silent vendor model rotation invisible to `llm_output_drift` (watches outputs) and embedding_migration framework (handles INTENTIONAL swaps). **§49.5 Q14.5 interest-model ossification** (`app/healing/monitors/interest_ossification.py`): three signals — Shannon entropy <0.30 (concentrated) / >0.90 (diffuse) / Jaccard ≥0.95 for 4 weeks (ossified). Each routes distinct Signal topic; emits `interest_ossification` landmark on any alert. **§49.6 Q14.6 lock contention** (passive instrumentation): new `app/utils/lock_metrics.py:time_write(resource)` context manager records writes ≥50ms to `workspace/healing/lock_waits.jsonl` (capped 10k via append_with_cap). `safe_io.safe_write` + `safe_io.safe_append` wrap inner `_do_safe_*` helpers in `time_write` — zero behavior change. New `app/healing/monitors/lock_contention.py` weekly p99 monitor alerts at ≥500ms. **Wiring**: 4 new IDENTITY_EVENT_KINDS (now 17→18 with parallel Q15.1); 6 new runtime_settings master switches (all default ON); 5 new healing monitors. Production cadence-gate bug fixed in 4 monitors (`last_run_at=0` no longer falsely triggers gate). 28 new tests; Q7→Q14 regression: **307 pass + 6 skipped**. See `crewai-team/docs/RISK_REGISTER.md` for the complete spec.
- Q13 year-2+ resilience (2026-05-16; PROGRAM.md §48 — 3 missing items from §2 audit): operator's §2 audit named 8 year-2+ resilience classes. Audit: 5 already shipped (algorithm_pinning + crypto_rotation_drill, rolled_log audit_journal, version_upgrade_drill, provider_contract_drift, continuity_ledger), 3 missing. All 3 shipped. **§48.1 Q13.1 schema-migration drill**: new `deploy/scripts/migration-drill.sh` + `app/healing/monitors/migration_drill.py` (30th healing monitor). Restores freshest postgres-OK backup into scratch Postgres → runs `app.memory.startup_migrations.apply_all` (the same code production runs at boot — idempotent, every operation guarded with `IF NOT EXISTS`) → smoke-queries tables today's code expects (`epistemic_peer_reviews`, `epistemic_overrides`, `error_anomalies`, plus the older `audit_log`, `crew_tasks`, `tickets` baselines). Monitor alerts on 4 states (never_run / malformed / stale at 100d / failed) + emits `schema_migration_drill` ledger landmark. **Redesigned 2026-05-16**: the original implementation walked `migrations/0*_*.sql` and tracked applied ones in a fabricated `control_plane._schema_migrations` table — `grep -rn "_schema_migrations" app/ migrations/` returns zero hits, so the table was an invention of the drill and the tracking was always empty. As a result every drill re-applied every migration end-to-end against a fully-restored schema; 4 migrations (002, 003, 016, 019) are not idempotent on a fully-migrated schema, so they failed and the drill permanently recorded `last_drill_ok=false`. The redesign drops the walk entirely and drills the production code path — which is the honest test of "today's code can't read a 6-month-old backup" (the user's stated §2.2 concern): a backup taken after a migration was applied in production contains the resulting schema, so the smoke passes; a backup taken before a never-shipped-in-production migration won't have the new tables, so the smoke fails — the operator-visible signal. **Isolation hardened 2026-05-16 post-incident**: first-run validation revealed an inherited bug — `docker compose -p andrusai-migration-drill` renames containers/networks but reads bind-mount paths LITERALLY from `docker-compose.yml`, so the drill's postgres mounted the same `./workspace/mem0_pgdata` as the live stack and the partial `psql` restore corrupted the live catalog (~70 min of writes lost; restored from 13:58 UTC backup). The PR now ships two compounding fixes: (a) **compose overlay** `docker-compose.migration-drill.yml` using `!override` (Compose v2.24+) to remap postgres to an ephemeral named volume `drill_pgdata` and downgrade gateway sidecar mounts to read-only; (b) **pre-flight refusal** — exit 4 if `crewai-team-postgres-1` exists in any state, belt-and-suspenders for the case where the overlay is accidentally removed. **§48.2 Q13.2 dependency_radar** (`app/dependency_radar/` ~700 LOC): weekly HEAVY idle. Three signals — `pip list --outdated --format=json` subprocess, OSV.dev `/v1/querybatch` POST, GitHub `repos/{owner}/{repo}` pushed_at. Routes: PATCH → `proposal_bridge.stage()` (7d cooldown) → CR; MINOR → 14d cooldown CR; MAJOR → Signal alert only; CVE-with-fix → 3d cooldown CR; CVE_NO_FIX / ABANDONED → Signal only. Added `"dependency_radar"` to `proposal_bridge.store._KNOWN_SOURCES`; boot-anchored via `app/healing/__init__.py`. `_MAX_PROPOSALS_PER_PASS=3` matches proposal_bridge rate limit. **§48.3 Q13.3 tz_drift monitor** (`app/healing/monitors/tz_drift.py`, 31st healing monitor): three daily probes — now-offset, March-equinox-anchor, October-equinox-anchor. Compares re-implemented hand-rolled `_helsinki_tz()` vs `ZoneInfo("Europe/Helsinki")`; ANY divergence > 60s → Signal alert + auto-CR proposing consolidation onto ZoneInfo (sister sites `affect/hooks.py` + `companion/scheduler.py` already use ZoneInfo) + emits `tz_drift` landmark. Recovery transition also landmarked. **Wiring**: 2 new IDENTITY_EVENT_KINDS; 3 new runtime_settings master switches (all default ON). 31 new tests in `tests/test_q13_resilience_year2.py`; Q7→Q13 regression: **279 pass + 6 skipped**. See `crewai-team/docs/DEPENDENCY_RADAR.md` + `crewai-team/docs/RESILIENCE_DRILLS.md` (extended with the Pattern A vs B distinction).
- Q12 self-understanding & philosophy (2026-05-16; PROGRAM.md §47 — 5-item §8 audit + 8.4 ship): operator's §8 spec broke into 5 items. Audit: 4 already shipped (§8.1 `app/subia/inquiry/`, §8.2 `app/identity/annual_reflection.py`, §8.3 `app/identity/continuity_ledger.py`, §8.5 `app/identity/legacy_essay.py`); §8.4 missing. **§47.1 Q12.4 sentience-probe self-design** (`app/subia/probe_proposals/` + `app/tools/probe_proposal_tools.py`): two-stage architecture — Stage 1 (agent-callable) files a markdown DESIGN doc CR at `docs/proposed_probes/<slug>.md`; Stage 2 (operator-only, follow-on) is a separate Tier-3 amendment that adds the actual `eval_*` function under `app/subia/probes/`. The agent CAN propose new candidate indicators; CANNOT modify existing probes / grade itself / redefine reserved Butlin or RSM anchors (19 reserved names including exact-match + family-prefix guard so `AE-2-extended` is refused) / make phenomenal claims (PhenomenalLanguageLinter HARD_FAIL). Eight validation checks: name discipline → field length (30–2000 chars) → forbidden-path discipline → phenomenal-language linter → 90-day cooldown → markdown render → CR via `change_requests.lifecycle.create_request` → best-effort `sentience_probe_proposal` ledger event. CrewAI BaseTool wrapper auto-discovered via `TOOL_MODULE_ROOTS = ("app.tools",)`. **Pinning test** `test_reserved_anchors_match_butlin_probe_set` reads butlin.py source and asserts every anchor referenced is in proposer's `BUTLIN_ANCHORS` — indicator namespace cannot drift between probe code and refusal list. **Wiring**: 1 new IDENTITY_EVENT_KIND (`sentience_probe_proposal`); SubIA integrity manifest unchanged (additive-only — 2 new files under `app/subia/`); no TIER_IMMUTABLE files touched. 24 new tests; Q7→Q12 regression: **248 pass + 6 skipped**. See `crewai-team/docs/PROBE_PROPOSALS.md`.
- Q11 creative synthesis (2026-05-16; PROGRAM.md §46.18–§46.24 — five threads): audit found three orphaned primitives (analogy_index, concept_blend, novelty_wrap shipped pre-Q11 but never consumed), one missing (idea_evolution), one unwired (aesthetics KB). Q11 wires everything into the brainstorm subsystem. Operator decisions: (1) populator default ON, flippable from React; (2) idea_evolution standalone module — NOT shinka package coupling (the spec referenced the pattern, not the library); (3) novelty tag-only; (4) aesthetic score-in-report, no auto-action. **§46.18 Q11.1 analogy populator** (`app/creativity/analogy_populator.py` ~280 LOC): HEAVY weekly idle job walks wiki/*.md + episteme `list_texts()` for unprocessed sources; Anthropic Haiku 4.5 extracts one abstract structural pattern + ≥2 cross-domain examples per source; cap `_MAX_NEW_PER_PASS=5` (~$0.25/week worst case); SHA-256-keyed state at `workspace/creativity/analogy_populator_state.json` makes re-runs idempotent; failure-isolated per source. Runtime setting `analogy_index_populator_enabled` (default ON) + AnalogyIndexCard in /cp/settings. **Brainstorm consumer**: `BrainstormSession.analogues` field (back-compat); `facilitator.start` queries `query_analogies(topic, top_k=3, min_similarity=0.15)`; `_inject_analogues_into_prompt` prepends a "Cross-domain analogues (inspiration, not constraint)" block to the step prompt for the multi-agent seed/react rounds. **§46.19 Q11.2 concept-blend technique** (`app/brainstorm/techniques/concept_blend.py`): registers as 8th technique with 4 steps; overrides `next_prompt` for step 3 to invoke `creativity.concept_blend.blend_concepts(input_a, input_b)` and splice the BlendResult's generic structure + emergent properties + follow-on questions into the prompt; failure-isolated. **§46.20 Q11.4 novelty in report**: `report._annotate_text` calls `creativity.novelty_wrap.assess_brainstorm_idea` and emits `{novelty: {verdict, ...}, aesthetic_score: ...}` per idea; Writer prompt gains an Annotation legend telling the LLM to flag — NOT drop — RESTATED and REJECTED_BEFORE ideas. **§46.21 Q11.5 aesthetic score** (`app/creativity/aesthetic_score.py`): wraps `app.aesthetics.vectorstore` with `score(text, top_k=3) → Optional[float]` returning `mean(top_k_similarity) × mean(top_k_quality_score)`; normalises curated quality (0..10 → 0..1); None sentinel when store empty/unavailable. **§46.22 Q11.3 idea_evolution standalone** (`app/brainstorm/idea_evolution.py` ~480 LOC): population + LLM mutation + LLM judge + diversity archive + budget guard, NO shinka package coupling. `IdeaMember` + `IdeaEvolutionResult`; `evolve_ideas(...)` with hard caps MAX_BUDGET_USD=2.0, MAX_GENERATIONS=20, MAX_POPULATION=12; per-call cost constants (judge $0.0005, mutate $0.001) over-estimate deliberately so budget guard fires early; `_diverse_pick` (greedy max pairwise distance via hash-trick cosine) preserves diversity; default Anthropic Haiku 4.5 mutator + judge with deterministic seams for tests. 30 new tests; Q7→Q11 regression: **262 pass + 6 skipped**. **§46.24 deliberate non-decisions**: no tech_radar refactor (analogy_populator reads corpus directly); no auto-idea-drop in brainstorm (tag-only); no aesthetic threshold (score-only); no `/brainstorm evolve` slash command yet (module shipped + tested; operator-facing wiring deferred); no JSON→ChromaDB switch for analogy_index (current JSONL with hash-trick embedding is fine until 10k+ entries).
- Q10 tech-radar → adoption loop (2026-05-16; PROGRAM.md §46.13–§46.17 — three threads): post-§46 audit of the discovery → adoption gap. **6.1 trial-canary-adopt** had a partial primitive — `library_radar.proposer` discovered + filed markdown proposals with coding_session_spec YAML, but no actual trial runner. Q10.1 closes it via `app/library_radar/trial_state.py` (append-only JSONL ledger; idempotent mark_pending + state transitions) + `app/library_radar/trial_runner.py` (inline LIGHT idle job hooked into the proposer's daemon loop; per-pass cap of 3 trials to avoid coding-session quota exhaustion; smoke-test ONLY for v1 — `render_smoke_test(pkg, slug)` generates a deterministic `import` + public-attribute assertion, never LLM-supplied Python that the sandbox will execute; on exit 0 files an adoption CR for `requirements.txt` via `change_requests.lifecycle.create_request`, standard operator gate intact). Master switch `LIBRARY_TRIAL_RUNNER_ENABLED` (default ON). **6.2 paper code-from-paper** added `codeable: bool` + optional `scaffold: {driver_purpose, acceptance, duration_min}` to the `paper_pipeline` LLM JSON output; `_build_coding_session_spec` returns None when `codeable=false` (theoretical/hardware/large-scale papers stay prose-only); duration_min clamped to [5, 240]; JSONL ledger row gains `codeable`, `scaffold`, `source` fields; daily briefing `_gather_codeable_papers(n=3)` surfaces top codeable rows newest-first in the morning briefing as "💻 Queued codeable paper ideas" (soft fail). **6.3 multi-source feeds** new `app/episteme/feed_sources.py` adds 4 adapters: `fetch_openreview(venues=NeurIPS/ICML/ICLR)` via the public OpenReview API with v1/v2 content-shape handling, `fetch_python_peps` (peps.python.org RSS, 90d lookback), `fetch_w3c_tr` (W3C News atom, 30d), `fetch_huggingface_papers` (HF papers atom, 14d). Per-source master switches (`PAPER_PIPELINE_{OPENREVIEW,PEPS,W3C,HF}_ENABLED`, all default ON). Every public wrapper is try/except-isolated so a broken source can't take down the others. `fetch_extra_sources()` dedupes by id across sources; `paper_pipeline.run()` extends `papers` with the extras BEFORE the dedup+summarize loop so the existing arXiv pipeline handles all sources uniformly. 24 new tests in `tests/test_q10_tech_radar_loop.py`; Q7+Q8+Q9+Q10 regression: **221 pass + 6 skipped**. **Deliberate non-decisions** (§46.17): no LLM-supplied smoke-test body (template-generated to avoid hallucinated API calls in the sandbox), no per-library micro-benchmark slot for v1 (smoke test only), no auto-apply for adoption CRs (standard operator gate), no auto-execution of queued codeable paper ideas (daily briefing surfaces them; operator chooses), no `tech_radar_crew` changes (parallel feed sources without modifying the existing 7-query generic web-search crew).
- Q9 companion deepening (2026-05-16; PROGRAM.md §46.6–§46.11 — four threads + two declined): post-§46 audit of the operator's "knows me well" criteria. **5.1 health** already shipped (`app/health/`, PROGRAM §34.1). **5.2 financial** deferred per operator decision. **5.3 travel** (`app/life_companion/travel.py` ~360 LOC): hand-rolled minimal iCal parser (no `icalendar` dep) for TripIt `.ics` feeds (`TRIPIT_ICAL_URL` env); segment kind detection (flight / ferry / train / hotel / car) with Helsinki-Tallinn ferry vendor patterns; Aviationstack flight-status (free 100/mo quota, capped at 3 calls/cycle); 6h cadence-guarded idle job; daily-briefing "🛫 Travel" section auto-hides when nothing in window. **5.4 inbox handlers** (`app/inbox/handlers/`): 5 modules replacing the `_handle_unsupported` stubs — `audio_transcribe` (200 MB cap → `app.voice` → notes), `image_vision` (Anthropic Haiku 4.5 → bullet-extract markdown), `pdf_extract` (Haiku 4.5 `document` content block → strict-JSON output → routes receipts to `workspace/finance/expenses.jsonl` vs documents to notes), `spreadsheet_to_csv` (openpyxl/odfpy → 50k-row cap → notes), `youtube_link` (delegates to existing `SelfImprovementCrew.learn_from_youtube`). Classifier extended with `youtube_link` kind detected by content-peek on `.url`/`.webloc`/`.txt` files containing a YouTube URL. **5.5 action handlers** (`app/action_requests/handlers/{calendar_invite,signal_send}.py`): two new `ActionType` enum values; calendar_invite uses `app.google_workspace.get_service("calendar")` + `events().insert(sendUpdates="all" if attendees)`; signal_send uses `send_message_blocking` so apply artifact carries the Signal timestamp for reaction-correlation. Both register via try/except guards so a broken handler doesn't take down the others. **5.6 long-term goal review** (`app/identity/long_term_goal_review.py` ~450 LOC + `app/control_plane/goals_api.py`): mirrors `annual_reflection` pattern. Reads 5 sources (current_goals from SelfState, grand_task events, completed crew tasks, closed Q8 threads, identity-continuity drift) → composer LLM call with phenomenal-language linter retry → `wiki/self/quarterly_reviews/<year>_q<n>.md`. 85-day minimum interval; `/goals` + `/goals review` + `/goals list-reviews` Signal commands; `GET /api/cp/goals/{state,reviews}` + `POST /api/cp/goals/review` REST. Wired into identity scheduler as `identity-long-term-goal-review` (LIGHT). **Deliberate non-decisions** (§46.11): no financial subsystem (operator deferred), no Slack handler (no Slack client wired), no Plaid (US-focused), no current_goals mutation from review (observational only — proposes amendments in a dedicated section, never writes). 29 new tests; Q7+Q8+Q9 regression: **191 pass + 6 skipped**.
- Q8 difficult-question solving (2026-05-16; PROGRAM.md §46 — three threads): post-§45 review surfaced three areas where the spec called for behavior the system didn't fully deliver. **4.1 long-horizon threads** — the `app/threads/` primitive already shipped pre-Q8 (models + 5-state lifecycle + REST + React + 21 tests), but three operator-facing pieces were missing. **4.2 failure-replay** — `lessons_learned.py` operated on rejected CRs + thumbs-down feedback + Goodhart signals, but had no thread-closure hook. **4.3 tool composition** — genuinely greenfield; skills are one-task-template, brainstorm is hardcoded Python state machines, Forge synthesises new tool source. Operator decisions before kickoff: (1) JSON-per-record threads storage (matches existing pattern); (2) cheap-tier LLM closure synthesis + deterministic fallback; (3) async workflow queue + status endpoint; (4) brainstorm-techniques refactor: doc-only mention. **§46.1 Q8.1 threads completion** — `Thread.unblock_hints: list[str]` symmetric to `blockers` ("what MIGHT resolve the block"; read by recovery loop); `add_unblock_hint` / `clear_unblock_hints` lifecycle + REST endpoints; new `/thread {start, status, list, note, subq, done, block, unblock, hint, resolve, abandon}` Signal slash command family with 11 SignalCommand rows under new `Threads` category + `_handle_thread_command` dispatcher (mirrors `/tensions`/`/person`/`/skill` pattern); 8-char short-id resolution; new `app/recovery/thread_consultation.py` surfaces open-thread payloads (title, status, blockers, top hints) into recovery `ctx["thread_hints"]` — strategies may consult, never forced; 5-thread + 240-char-per-string truncation guards prompt budget; failure-isolated (broken threads module → empty list); `useAddUnblockHintMutation` + `useClearUnblockHintsMutation` hooks for React. **§46.2 Q8.2 thread-closure approaches-tried** — new `app/threads/approaches.py` module: `distill_on_closure(thread)` walks notes + blockers + sub-questions + unblock_hints, builds structured deterministic body (`_build_deterministic_body`), optionally upgrades via Anthropic Haiku 4.5 (`APPROACHES_LLM_ENABLED` env default ON; ~$0.0001 per closure), sets `thread.approaches_summary` (2000 char cap); emits `source="thread_closure"` event into the `lessons_learned` KB via public `_cluster_into_kb` helper (singleton thread-closure clusters kept — every closed thread is itself worth preserving); `consult_before_create(title, description)` runs on `create_thread` and fires a one-shot Signal notification when any match has similarity ≥0.6 (top 3 lessons, `topic="thread_consult:<id>"` for arbiter dedup); `resolve_thread` and `abandon_thread` both invoke `_distill_on_closure_safely` after persisting the closure transition (failure-isolated: distiller never rolls back lifecycle). **§46.3 Q8.3 workflow_template primitive** — new `app/workflows/` package (~700 LOC: models / validator / store / executor / queue): `WorkflowTemplate` (id + name + description + nodes + inputs + counters) → `WorkflowNode` (id + tool_name + args + depends_on + description) + `WorkflowRun` (status + node_outputs + node_statuses) with `RunStatus` enum (queued / running / succeeded / failed / cancelled); 7-check validator (non-empty id+name → unique node ids → depends_on siblings → Kahn-cycle check → `${node.field}` topologically earlier → `{input}` declared in `template.inputs` → tool registered if registry available); JSON-per-record store at `workspace/workflows/templates/<id>.json` + `workspace/workflows/runs/<id>.json`; executor with topological order + reference resolution + 8000-char output truncation + per-status persist (crash mid-run leaves partial record); two regex passes (`${node.field}` first, then `{input}`) with negative lookbehind on `$` so `${name}` doesn't double-match; failure propagation marks downstream nodes `skipped` (not blank) so run record reflects full impact; tool dispatch tries registry first, then `app.tools.<tool_name>` direct callable; `ThreadPoolExecutor`-backed queue at `_MAX_CONCURRENT_RUNS=4` (matches gateway's single-process pattern). REST: 8 endpoints under `/api/cp/workflows/*` (templates CRUD + per-template `/run` + `/runs` list + `/runs/<id>` poll + `/runs/<id>/cancel`). Agent tools `workflow_run` + `workflow_list` exported (not auto-registered into default inventories — operator-wired per agent when relevant). React `/workflows` page with KPI cards + per-template Run drawer with live status (3s refetch). **§46.4 Q8 regression** — 162 pass + 6 skipped (gateway-deps) across Q7 + Q8 + legacy threads + legacy consolidation. **§46.5 deliberate non-decisions**: no Postgres migration of threads (JSON matches the four-primitive pattern); no brainstorm refactor (state machines work fine); no DAG visual editor (JSON-via-REST is canonical for v1); no conditional branches (no expression language); no agent-inventory auto-registration of workflow tools (avoids tool-bloat).
- Q7 self-evolution closure (2026-05-15/16; PROGRAM.md §45 — four threads): post-§44 review surfaced four places where the operator's spec promised behavior that wasn't fully shipped. Operator decisions before kickoff: (1) adoption auto-rollback switchable in React, **default ON**; (2) schema-change detection **path-only** for v1; (3) recipe consolidation **keep both** triggers. **§45.1 Q7.1 — architecture-request adoption** (`app/architecture_requests/adoption.py`, ~400 LOC): 4-signal saturating-weighted-sum probe (imports×0.40 + idle_runs×0.30 + outputs×0.20 + operator_interactions×0.10; each signal saturates at 5/10/5/3 to prevent a runaway from dominating). `measure(request_id)` runs only in the 30–60d post-APPLIED window; the four signal probes individually failure-isolated. New 29th healing monitor `app/healing/monitors/architecture_adoption.py` files a rollback CR via `change_requests.lifecycle.create_request` when score <0.20 with per-request flag-file dedup; topic-keyed Signal alert `arch_adoption_low:<id>`. **Never auto-applies** — "auto-rollback default ON" means the PROPOSAL is automatic; the APPLICATION still goes through the operator gate. Two new master switches in `runtime_settings.py` (both default ON): `architecture_requests_enabled` (top-level kill switch; `lifecycle.py` raises `ProtocolDisabled` when OFF) + `architecture_adoption_monitor_enabled` (independent control of the probe only). React `ArchitectureRequestsCard.tsx` under `/cp/settings`. **§45.2 Q7.2 — schema-aware coding-session submit** (`app/coding_session/schema_migrations.py`, ~180 LOC): curated `_SCHEMA_OWNING_PATHS` allow-list (path-PREFIX match; adding an entry is a deliberate decision — false positives manifest as rejectable CRs, never silent corruption). `detect_schema_changes` returns a `SchemaChangeHint` with detected paths + inferred snake-case name (`control_plane_db` for `app/control_plane/db.py`, `amend_migration_<stem>` for edits to existing migration files) + next `NNNN+1` number + suggested filename. `render_migration_stub` emits a `BEGIN;`/`COMMIT;` SQL stub with explicit `TODO` markers and an idempotency reminder. `submit_session` now has a `_submit_one_synthesized_file` helper that files the stub as a CR with `old_content=""` (synthesized — there's no diff). The hook is failure-isolated (broken detector never blocks the regular per-file CRs). **No alembic introduced** — matches the existing `migrations/NNNN_*.sql` convention applied by `app/memory/startup_migrations.py`; adding alembic would have created parallel migration systems. **§45.3 Q7.3 — recipe-consolidation selection-rate trigger** (`app/self_improvement/meta_agent/consolidation.py`): the pre-existing health-score trigger (weekly, four-term composite) catches QUALITY decay; the new selection-rate trigger (quarterly, <5% over 90d, n_offered≥20 sample-size gate) catches RELEVANCE decay — operators don't pick a recipe even when its win-rate looks fine. `compute_selection_rate` has three-level shape fallback (`offered_at_history`/`selected_at_history` per-event lists → `times_offered_90d`/`times_selected_90d` precomputed counters → `times_offered`+`uses` conservative lifetime fallback that over-counts offered so rate runs HIGH = fail-open against false positives). `run_one_pass()` runs both triggers; within a single pass, health-score wins precedence via a `proposed_in_pass` dedup set (a recipe failing both signals gets exactly one row; the other surfaces on next pass). Result dict gains `n_selection_rate_evaluated` for operator visibility. New `mark_superseded_by(recipe_id, *, superseded_by, operator_actor)` operator-approved retirement writer — prefers `store.mark_recipe_superseded` if present, falls back to `get_recipe`+manual mutation+`save_recipe`; the recipe stays in the ledger (audit trail preserved), only `is_active` flips False. **Boot anchor**: `app/healing/__init__.py` now imports the consolidation module — the daemon eager-started at module-import time but nothing in the boot chain was importing it (same gap §38 closed for the five other observational subsystems; Q7.3 extends the same fix). Q1→Q7 regression: 69 pass + 6 skipped (gateway-deps) across the three new test files. No TIER_IMMUTABLE files modified. **§45.4 Q7.4 — inline ShinkaEvolve operator-visibility closure** (added on follow-up after user asked "why deferred?"): the bridge `app/coding_session/evolution_bridge.py` + agent tool `coding_session_evolve_solution` already shipped pre-Q7.4 (hard caps 20 gen / 3 islands / $5; TIER_IMMUTABLE + `app/subia/` + `goal_emitter.py` refusals; graceful `shinka_unavailable` fallback). Q7.4 adds the operator-visibility layer that was missing: **per-session audit JSONL** at `workspace/coding_sessions/<id>/evolution_audit.jsonl` (lives OUTSIDE the worktree so it survives session cleanup; one row per call on every return path — refused/disabled/error/shinka_unavailable/no_improvement/improved; stores diff SHA-256 + length, NOT the diff itself; capped 200 rows; path-traversal-guarded; failure-isolated); **master switch `shinka_inline_evolve_enabled`** in `runtime_settings.py` (default ON; OFF → bridge returns `status="disabled"`; failure-isolated fail-open if runtime_settings unavailable so the bridge's own safety layers stay the boundary); **REST endpoint** `GET /api/cp/coding-sessions/<id>/evolution_runs?limit=N` → `{session_id, summary, runs}`; **React `InlineEvolveCard.tsx`** in `/cp/settings`; **`EvolutionRunsSection`** auto-rendered on the `/cp/coding-sessions/<id>` detail drawer (KPIs + status-coded per-run rows; hides for sessions with zero evolution runs). 15 new tests in `tests/test_q7_4_inline_evolve.py`. **§45.5 deliberate non-decisions**: no new safety constraints (existing refusal layers sufficient); no full-diff persistence in the audit (only SHA-256 + length — full diff is reconstructable from post-submit CR audit if/when the agent submits); no auto-submit (the agent still calls `coding_session_write` + `coding_session_submit` → standard per-file CR fanout — distinct from Q7.2's operator-completes-the-SQL migration stubs); no bulk-engine integration (`app.shinka_engine` has its own master switch + React surface; the two ShinkaEvolve usage modes are deliberately separate). **§45.6 capability-gap learner** already shipped under `app/self_improvement/capability_gap_analyzer.py` (§32.5) + integrated into the proposal bridge (§38.1).
- Q4 Companion depth (2026-05-11; PROGRAM.md §41): three additive items deepening the companion surface. **§41.1 Companion tensions store** (`app/companion/tensions.py`) — open questions Andrus left with the companion, tracked on his behalf. Distinct from SubIA wonder (internal contemplation). OPEN/DORMANT/RESOLVED lifecycle + freshness decay (30d halflife) + 90d-untouched auto-DORMANT + 30-OPEN cap + regex detection from text. One JSON file per tension at `workspace/companion/tensions/<id>.json`. Daily briefing "❓ Open questions you left with me" section. REST: `GET/POST /api/cp/companion/tensions`. Signal: `/tensions`. React `TensionsCard.tsx` in /cp/companion. **§41.2 Cross-modal pattern detector** (`app/companion/cross_modal_patterns.py`) — convergence signal across 6 modalities (interest_model's 5 sources + tickets). Strength = modality_factor × log-scaled volume. Threshold: ≥3 modalities × ≥8 hits × strength ≥0.7 × 21d window. Persists to `workspace/companion/cross_modal_patterns.jsonl` with archive rotation. Cross-links to tensions: matching patterns boost freshness. GW publish on detection. Daily briefing "💡 Proactive insights" section. React `CrossModalPatternsCard.tsx`. **§41.3 Surface arbitration** (`app/notify/arbiter.py` + `app/notify/fatigue.py`) — opt-in "is this worth interrupting?" pre-filter on `notify()`. Three decisions (send_now / queue_for_digest / suppress_low_value); inputs: interest_score + pattern_boost + tension_boost + welfare-breach guard + fatigue. Critical bypass (`critical=True` always sends). Goodhart guards: 30% daily suppression-rate ceiling forces-send, weekly `notify_suppression_review` (27th healing monitor) digest. `notify(arbitrate=True, topic=…, critical=…)` opt-in — existing call sites unchanged. SubIA wonder NOT bridged (kept deliberately separate). 20 new tests; Q1+Q2+Q3+Q3.1+Q3.2+Q3.3+Q1.4+Q4 regression: **133 pass + 56 skip, 0 fail**.
- Q3.3 fourth-pass cleanup (2026-05-11; PROGRAM.md §40.3): final ultrathink pass — genuine edge-cases that survived three prior audits. **§40.3.1** Restart-claim check moved to first step of lifespan (was after gateway-bind check + audit-log config) for defense-in-depth — even if bind check raises, operator sees restart alert first. **§40.3.2** Cutover idempotency: `propose_cutover()` now guards against duplicate Tier-3 proposals via `_existing_active_proposal_for(plan_id)` that scans non-terminal states. **§40.3.3** Budget forecast excludes paused agents (`COALESCE(is_paused, false) = false` in the aggregate-limit SQL) — paused budgets shouldn't inflate headroom. **§40.3.4** DR secret denylist extended with `.netrc`, SSH private keys (`id_rsa*`/`id_ed25519*`/`id_ecdsa*`), AWS creds, KeePass `.kdb`/`.kdbx`, GPG keyrings `.gpg`/`.pgp`/`secring.*`/`pubring.*`, and generic `.pem`/`.key` suffixes. **§40.3.5** `read_archive` per-file lock semantics — Q3.2 held the lock across the entire iteration (starving the rotator under slow consumers); Q3.3 acquires per-file and releases between, preserving lazy line-by-line yield within each file. **§40.3.6** Reclaim-trend alert dedup — first-in-series fires; subsequent passes still over threshold stay silent; resurface after 4 silent passes (~1 year quarterly) with `📈🔁 (repeat #N)` prefix; alert clears reset the dedup. 16 new tests in `test_q3_3_cleanup.py` (all pass); Q1+Q2+Q3+Q3.1+Q3.2+Q3.3 regression: **106 pass + 56 skip, 0 fail**. **Q3 closed for real after four cycles.**
- Q3.2 third-pass cleanup (2026-05-11; PROGRAM.md §40.2): post-Q3.1 ultrathink surfaced subtler issues; this pass closes edge-cases + adds operator safety nets. **§40.2.1 + §40.2.9 Generic restart-claim mechanism** (`runtime_settings.post_amendment_restart_claims` queue + idempotent `append_post_amendment_restart_claim` / `clear_post_amendment_restart_claims` / `get_post_amendment_restart_claims`; `main.py:_process_post_amendment_restart_claims` startup self-check that satisfies/clears claims when live module state matches, alerts loudly on unsatisfied) — closes the dim-mismatch gap between cutover.post_apply_hook (rewrites disk) and gateway restart (reloads _EMBED_DIM). Generic so future Tier-3 amendments with hot-reload-incompatible effects use the same mechanism. **§40.2.2 Pre-cutover auto-export**: `propose_cutover` now runs `app.dr.export_kbs.export(label="pre_cutover_<plan_id>")` as its first action after verification — belt-and-suspenders snapshot at point-in-time of cutover. **§40.2.3 Cross-rotation reader/writer flock** in `app/utils/jsonl_retention.py` (POSIX advisory lock on `.{name}.rotation_lock` sidecar; rotator LOCK_EX, readers LOCK_SH) — fixes a real race where `read_archive(include_live=True)` during a rotation could double-count entries. Best-effort across platforms; degrades to pre-Q3.2 behavior without fcntl. **§40.2.4 Welfare audit access verified** — `monotonic_drift_check` reads a different data source (l9_snapshots, not archive-rotated); no action needed. **§40.2.5 HNSW orphan segment detection** in `chromadb_hygiene` (scans UUID-named subdirs of each KB against the SQLite `collections` table; alerts when ≥10 MB orphans accumulate; never auto-deletes). **§40.2.6 Reclaim trend tracking** — rolling 4-pass history of `freed_total`; alerts when current pass ≥2× prior-median (signal that VACUUM isn't keeping pace; full rebuild needed). **§40.2.7 Annual reflection coverage verified** — `summarise_drift.by_kind` is a dynamic Counter; `substrate_migration` surfaces automatically in `wiki/self/value_reflections/<year>.md`. **§40.2.8 Multi-layer migration warning** in `docs/EMBEDDING_MIGRATION.md` — documents the 4-layer surface (ChromaDB memory wired / ChromaDB other KBs refused / pgvector not wired / Neo4j inherits) + restart requirement. 17 new tests in `tests/test_q3_2_cleanup.py` (+ 4 skipped for pydantic_settings); Q1+Q2+Q3+Q3.1+Q3.2 regression: **90 pass + 56 skip, 0 fail**. After three ship cycles, Q3 has crossed into diminishing returns — further ultrathink passes would surface polish + future-readiness items at noise level. **Q3 declared done.**
- Q3.1 cleanup pass (2026-05-11; PROGRAM.md §40.1): closes three real defects identified in the post-Q3 ultrathink audit. **§40.1.1a Plan validator** (`embedding_migration/plan.py:validate_plan`) refuses unsupported targets (pgvector + non-memory chromadb KBs) at save AND load — the Q3 framework had accepted them and silently routed to the wrong place. **§40.1.1b KB-rooted chromadb client** (`chromadb_manager.get_kb_client(kb_name)` + `_kb_clients` registry; `dual_write._matching_target`) routes shadow writes through the plan target's KB. **§40.1.1c Archive-walking readers** — salience `load_recent` / welfare `read_audit` / care_policies `read_care_ledger` / decentered `_load_*` / calibration `load_recent_trace_with_viability` all escalate to `read_archive` when the requested window extends past the live file (early-exit-guarded). Without this, the Q3 archive rotation preserved consciousness data nobody could read. **§40.1.1d Lazy-resolve paths** — `dr/`, `embedding_migration/plan.py` now honor a custom `WORKSPACE_ROOT`. **§40.1.2 Integration coverage**: new `substrate_migration` continuity-ledger event kind emitted from `cutover.post_apply_hook`; GW publish from chromadb_hygiene (on >1 MB reclaim), cutover (high salience 0.85), boot_drill (0.30 pass / 0.85 fail); boot_drill emits `integrity_regen` on successful pass. **§40.1.3 Robustness**: per-collection idempotency in `cutover.post_apply_hook` (`skipped_already_swapped` field); SHA-256 round-trip verification in the DR drill (export records hash per ledger file; drill recomputes + flags mismatch; backward-compat with pre-Q3.1 manifests). **§40.1.4**: `budgets.forecast_breach_periods` + `GET /api/cp/budgets/forecast` cross-references the cost-trend forecast against current budget caps. 17 new tests in `tests/test_q3_1_cleanup.py` (all pass); Q1+Q2+Q3+Q3.1 regression: 73 pass + 52 skip, 0 fail. No TIER_IMMUTABLE files modified.
- Multi-year hygiene — Q3 (2026-05-10/11; PROGRAM.md §40): five additive items closing long-tail "runs for years and then this becomes a problem" gaps. **§40.1 ChromaDB hygiene** — `app/healing/monitors/chromadb_hygiene.py` (25th monitor, quarterly SQLite VACUUM on every `chroma.sqlite3`); `app/memory/chromadb_rebuild.py` (operator-initiated HNSW reclaim with snapshot + `--dry-run` + `--from-snapshot` recovery). **§40.2 JSONL archive rotation** — `app/utils/jsonl_retention.py` extended with `append_with_archive_rotate` / `read_archive` / `archive_stats`; wired into 5 writers (affect trace/salience/welfare/care archive forever to monthly buckets; adapter_lifecycle uses simple cap). Decade-scale safe for HOT-1 / decentered-reflection / backward-counterfactual replay probes. **§40.3 Embedding-model migration framework** — new `app/memory/embedding_migration/` package (8 modules: plan / state / dual_write / shadow_read / verify / cutover / dry_run + init); 10-state machine over `runtime_settings.embedding_migration_state`; `chromadb_manager.store` + `retrieve` gain best-effort dual-write + shadow-read hooks (default OFF, swallow on failure). First production consumer of the Tier-3 amendment protocol (§25.1) — `cutover.propose_cutover()` builds the `_EMBED_DIM` patch and proposes via `governance_amendment.protocol.propose_amendment`. 4 new runtime_settings keys + `POST /api/cp/settings` toggles + `GET /api/cp/embedding-migration` one-shot status. **§40.4 DR drill** — `HEALING_DB_BACKUP_ENABLED=true` flipped; new `app/dr/` package (`export_kbs` / `import_kbs` / `boot_drill`) producing container-independent tarball with aggressive secret denylist (`.env`, `secrets/`, `google_token.json`, `vapid_*.pem`, anything matching `token`/`credential`/`private_key`); `scripts/dr_boot_drill.sh` shell wrapper; `docs/DR_DRILL.md` operator runbook. Boot drill validates restore against ephemeral target dir — never touches live workspace. **§40.5 Cost trend dashboard** — `app/control_plane/cost_trends.py` (pure-stdlib OLS regression on monthly totals from audit_log, 95% CI band, rolling z-score anomaly detection); `GET /api/cp/costs/trends` one-shot endpoint; `dashboard-react/src/components/CostTrendsCard.tsx` with history+forecast Line chart + KPI cards + anomalies table. Goodhart-resistant: forecast is observational, system never trains against it. 22 tests in `tests/test_q3_multiyear_hygiene.py` (all pass); Q1+Q2+Q3 regression: 43 pass + 52 skip (gateway-deps). See `crewai-team/docs/EMBEDDING_MIGRATION.md` + `crewai-team/docs/DR_DRILL.md`.
- Q6.5 second-cycle audit + Q6 closure (2026-05-13; PROGRAM.md §44.5): second post-ship audit on Q6 caught 1 P1 + 3 P2 polish + 3 doc gaps. All shipped. **Q6 declared closed**, trajectory has flattened to polish-only — matches Q5.6 closure pattern. **P1**: `scheduler.py:_notify_drill_failed` fires Signal alert on FAIL/ERROR drill outcomes immediately rather than letting the operator find out via the weekly briefing (up to 7 days late). Topic-keyed (`resilience_drill_failed:<name>`) for arbiter dedup. **P2**: `DrillRegistry.register()` now warns on different-module re-registration with different runner (silent on same-runner hot-reload — distinguishes accidental name collision from the Q6.2 hot-reload test pattern); new `app/healing/monitors/backup_freshness.py` (28th healing monitor) daily probes `workspace/backups/dr/` directory; alerts when newest `.tar.gz` is older than `2× POSTURE.target_backup_age_days` (14d default) — catches "operator's backup-sync cron silently died" without needing cloud SDKs (the LIGHT proxy that's adequate for the most common failure mode; real off-host integrity via S3/Google Drive remains operator-managed per posture decision); third per-drill stateless `kill_the_gateway` SPEC description now explicitly distinguishes master-switch-gates-scheduler-notifications from external-script-execution. **Docs**: `RESILIENCE_DRILLS.md` adds audit-log corruption recovery procedure (symptoms + Python diagnostic snippet + recovery: move corrupt file aside, drills repopulate, continuity ledger preserves landmarks), annual reflection consumption cross-reference (`summarise_drift.by_kind` Counter auto-surfaces the `resilience_drill` event kind into `wiki/self/value_reflections/<year>.md`), and FIVE specific re-open conditions for Q6 closure (live failure / posture violation / recovery-time excess / new drill class / concrete operator concern — NOT "let's verify Q6 is still ok"). 11 new tests; full Q1→Q6.5 regression: **443 pass + 62 skip, 0 fail**. Trajectory: §44.4 = 10 architectural findings → §44.5 = 6 polish/docs. Each cycle productive; next would produce noise-level items. After Q6.5: 28 healing monitors running, posture decision unchanged, Butlin scorecard `{STRONG=7, PARTIAL=3, ABSENT=4, FAIL=0}` unchanged.
- Q6.4 post-ship audit follow-ups (2026-05-13; PROGRAM.md §44.4): first audit-cycle pass on Q6 caught one P0 logic bug + 5 P1 + 3 P2 polish items, all shipped including the deferred-for-future list. **P0**: recovery-landmark emission was reading the audit log AFTER `append_result` so it saw its own row — `audit.emit_landmark_for` now takes `prior_status` as explicit kwarg; all 4 drills snapshot prior state BEFORE append and pass it through. Same Q5.5 lesson #2 recurrence — the test mocked the wrong layer. New `test_recovery_landmark_exercised_via_production_sequence` permanently encodes the production-sequence requirement in CI. **P1**: per-drill in-flight lock at `workspace/resilience/.<name>.lock` (1h stale TTL) prevents concurrent invocation; `is_first_run` uses `last_successful_for` not `last_result_for` so prior SKIPPED rows don't suppress first_pass landmark; boot-grace 7 days for `drill_staleness` monitor (audit-file-mtime heuristic); `inspect.getsource(bc)` replaces `open(bc.__file__).read()` for `.pyc`-only deployment compat; SOUL.md guard implemented as regex scan over serialized audit for full-length secret-shaped substrings (Anthropic `sk-ant-`, OpenRouter `sk-or-`, Bearer 32+) — drill ERROR with message that **never includes the matched value** (pinned). **P2**: `useDrillsRegistryQuery` hook + per-drill state badge in React card (green/amber/red/dim color coding by last status + staleness); new `app/resilience_drills/__main__.py` CLI with `list / run / posture / audit` subcommands; `kill_the_gateway` SPEC.description clarifies the toggle gates SCHEDULER notifications not external-script execution. **#11**: "Who watches the watchers?" doc section names the meta-question + practical mitigations (continuity-ledger gap visible in annual reflection, staleness monitor degraded-mode signal); no gateway-side meta-monitor by design (infinite-regress avoidance). 21 new tests in test_q6_4_audit_followup.py + 3 legacy test updates. Q1→Q6.4 regression: **432 pass + 61 skip, 0 fail**.
- Q6 — Resilience drills + posture decision (2026-05-13; PROGRAM.md §44 + `crewai-team/docs/RESILIENCE_POSTURE.md` + `crewai-team/docs/RESILIENCE_DRILLS.md`): two roadmap items unified — quarterly drills (#21) + the HA-vs-recovery posture decision (#22). **Posture (§44.0): the system commits to "good backup + fast bare-metal recovery", NOT high-availability — identity is data, not uptime.** Off-host backup policy: dual-target S3 + Google Drive, operator-managed. Decision encoded as `Posture` dataclass constants in `app/resilience_drills/posture.py` with `is_ha_proposed_for_subsystem` guard against HA-shaped naming drift; FIXED for v1 with four explicit escape conditions documented. **Q6.1 foundation**: new `app/resilience_drills/` package — `protocol.py` (DrillSpec / DrillResult / DrillRegistry / DrillRisk / DrillStatus enums + master/per-drill gate), `audit.py` (JSONL at `workspace/resilience/drill_audit.jsonl` + landmark emission to continuity ledger on FAIL / first-pass / recovery transitions only; routine PASS-then-PASS stays in audit log), `posture.py`. 10th continuity-ledger event kind `resilience_drill` added (annual reflection auto-surfaces via `summarise_drift` Counter). 6 master switches: `resilience_drills_enabled` (default ON), `drill_{backup_restore,embedding_migration,secret_rotation}_enabled` (ON), `drill_kill_the_gateway_enabled` (**OFF — operator opt-in**), `drill_staleness_monitor_enabled` (ON). **Q6.2 four drills**: `backup_restore` (LOW) wraps existing `app/dr/boot_drill.run_drill`; `embedding_migration` (LOW) wraps existing `app/memory/embedding_migration/dry_run.run_dry_run`; `secret_rotation` (LOW, DRY-RUN ONLY) verifies rotation procedure — gateway-secret generation, Bearer-token round-trip, per-agent `BRIDGE_TOKEN_<AGENT>` enumeration, vendor key format patterns — **NO secret values ever in audit log** pinned by `test_secret_rotation_never_leaks_secret_values`; `kill_the_gateway` (HIGH, DISRUPTIVE) architecturally split into pre-drill check (gateway) + external script (`scripts/drills/kill_the_gateway.sh` requiring typed phrase `"EXECUTE KILL DRILL"`) + post-drill ingest hook (companion loop detects report on first idle pass after recovery). Scheduler auto-runs LOW/MEDIUM cadence, NEVER auto-runs HIGH (operator runs script) — pinned by `test_scheduler_skips_high_risk_drills`. 27th healing monitor `drill_staleness` (daily probe; Q6.5 added `backup_freshness` as the 28th) alerts past-due drills via canonical `app.notify.notify`. **Q6.3 surfaces**: 4 REST endpoints under `/api/cp/drills/*` (registry / audit / run / posture); React `ResilienceDrillsCard.tsx` in `/cp/settings` with master toggle + 5 per-drill switches + explicit DISRUPTIVE warning on `kill_the_gateway` toggle; briefing weekly composer adds "🛡 Resilience drills (week)" section showing passes/failures/past-due (disappears when nothing actionable); `workspace/resilience/` added to DR export `_LEDGER_INCLUDES` so drill audit history survives bare-metal restore (per operator decision: drill audit is identity-shaping). Q1→Q6 regression: **411 pass + 61 skip, 0 fail**. Butlin scorecard `{STRONG=7, PARTIAL=3, ABSENT=4, FAIL=0}` unchanged. Q5.5 lessons applied from the start: every drill is in the registry + scheduled + surfaced; tests monkeypatch underlying wrapped functions not the drill runner itself; deleting any drill causes observable consequences (staleness alert + DR-export-test failure).
- Q5 — Targeted sentience experiments (2026-05-13; PROGRAM.md §43.1-§43.6 + `crewai-team/docs/SENTIENCE_EXPERIMENTS.md` + `crewai-team/app/sentience_experiments/`): three roadmap items (#18 + #19 + #20) shipped across six commits and four audit cycles; **Q5 declared closed at §43.6**. **Critical anti-Goodhart commitment**: the user's parenthetical descriptions for the four ABSENT Butlin indicators (AE-2 "rare-event causal credit assignment", HOT-1 "feelings-about-feelings", HOT-4 "metacognitive monitor on reasoning chain", RPT-1 "forward-prediction self-scoring with calibration") are **functional capability names**, NOT the literal indicator definitions. The codebase's `app/subia/probes/butlin.py` evaluates them strictly (AE-2 = embodiment, HOT-1 = generative perception, HOT-4 = sparse coding, RPT-1 = algorithmic recurrence — all architecturally unachievable on an LLM substrate). Q5 builds the *capabilities the user named*, never the *scorecard flags*. Modules live at `app/sentience_experiments/` precisely so the canonical-path evaluators (which check `app/subia/*`) don't see them. **Load-bearing pinning test** `tests/test_q5_2_modules.py::test_q5_does_not_change_butlin_scorecard` invokes every butlin evaluator and asserts the count remains `{STRONG=7, PARTIAL=3, ABSENT=4, FAIL=0}` with the targeted four still ABSENT — also surfaced live via `GET /api/cp/sentience/scorecard-pinning` (`anti_goodhart_intact: bool`). **The four modules**: `ae2_causal_credit.py` flags rare-event `(action_sig, outcome)` pairs at `outcome_density_ratio ≥ 3×` (Q5.4 renamed from misleading "lift"; reads errors + welfare + audit_log as outcomes); `hot1_meta_affect.py` detects temporal_cluster / recurring_trigger / sequence / baseline_drift / attractor_lock patterns over the FULL `workspace/affect/trace.jsonl` (not just welfare_audit breaches; Q5.4 fix) — **`decenter_text()` filter hard-rejects first-person affect prose** (SOUL.md guard, pinned by tests; LLM enrichment via canonical `anthropic.Anthropic` SDK pattern at Claude Haiku 4.5, decenter filter still the second guard); `hot4_metacog_monitor.py` is an async-batch summariser over `loadable_agent_usage.jsonl` producing per-step `confidence_proxy / cache_reliance / cascade_jump / unusual_score` — **signals are write-only telemetry** pinned by `test_hot4_signals_never_gate_dispatch_logic` (grep test with forbidden-substring fallback); `rpt1_self_calibration.py` registers forecasts → reconciles via named scorers (LLM-module scorers refused at registration) when `resolution_at` passes → 60d stale-timeout → computes per-kind Brier + ECE + 10-bucket curve — **calibration state never feeds back into the predictive layer** pinned by grep on `app/subia/prediction/`. RPT-1 producers wired at Tier-3 amendment proposal (`tier3_approval` forecast, treating `eligibility_failed` as terminal-False per Q5.6) AND CR creation (`cr_apply` forecast); both use `has_resolved_history` flag (Q5.5) to distinguish empty-history (uniform 0.5 prior) from proven-zero-success (clamped 0.1 prior). 4 LIGHT idle jobs in `companion.loop`. **Two infrastructure extensions**: `philosophy/dialectics.py` gains `consult_panel(question, traditions)` returning structured `PerspectiveTension` records (never prose), 7-day cache — wired into Tier-3 amendment proposals + identity-claim ratification (claims with ≥2 unresolved tensions deferred to Q4.1 tensions store) + post-apply welfare calibration; `identity/relevant_history.py` gains `classify_path()` (27-bucket taxonomy) + `relevant_history_by_kind(path, window_days=365)` with `counts_by_outcome` + `success_rate` + `has_resolved_history`. **Continuity ledger emission**: 9th event kind `sentience_observation` emitted from all four modules on LANDMARK events only (AE-2 dedup state capped at 10K with FIFO; HOT-4 7-day per-source cooldown via ledger consultation; AE-2 first-emission-per-pair via persistent state file). Annual reflection's `summarise_drift` Counter auto-surfaces the new kind. `ledger_bridge.emit_landmark` enforces per-process 50-emission ceiling per source_module. **Operator surfaces**: 5 REST endpoints under `/api/cp/sentience/*`; weekly briefing digest "🔬 Self-observation (week):" section with 7-day-bounded HOT-4 reads. **Master switches** (all default ON): `sentience_{ae2,hot1,hot4,rpt1}_enabled`, `philosophy_panel_enabled`, `ledger_governor_enabled`, `sentience_llm_hypothesis_enabled`. **HOT-1 ↔ decentered_reflection deliberate non-wire** (Q5.6): two complementary epistemic stances on the same data, kept separate. Q1→Q5.6 regression: **363 pass + 60 skip, 0 fail**. No TIER_IMMUTABLE files modified; no `app/subia/` files modified; SubIA integrity manifest unchanged. Trajectory of audit findings: Round 1 = 12 architectural; Round 2 = 6 architectural; Round 3 = 5 polish. Q5 closed — re-audit only on specific concrete concern (the CI pinning test + live audit endpoint cover routine verification).
- Person-correlation follow-ups (2026-05-11; PROGRAM.md §42.1 — Q4.2.1 + Q4.2.2): two parallel ultrathink-audit passes surfaced 12 findings against the just-shipped §42 stack — 6 bugs + 6 missing wires. **Q4.2.1 bugs**: L4.4 mute-suggestions filter (broken advertised feature: muting a person silenced L3 but not L4.4 bridge/weak-tie nudges); `recent_emitted` REST endpoint gated on master switch; graph_suggestions freshness check (`_is_fresh(data, max_hours=72)`) prevents stale-topology nudges; renamed `social_graph_dissolved.json` → `social_graph_dissolved_clusters.json` to match docs; `_gather_conversation_participants` time-bound via `AND ts >= ?`; per-briefing 24h re-emission cooldown via `_recent_emission_keys`. **Q4.2.2 wires**: 7th identity-ledger event kind `person_correlation_policy` emitted from L1/L4/L4.4 setters + `forget_all` + `forget_graph` (closes the multi-year integrity gap — annual reflection now picks up "this is the year we started tracking people"); promoted `arbiter._welfare_breaching` → public `welfare_breaching()` and gated `person_suggestions.generate_suggestions` on it (suppresses L3 emission under critical affect window); `cross_modal_patterns.detect_person_patterns()` reuses the convergence detector over `PersonProfile.occurrences_per_modality` with `kind="person"` discriminator on the JSONL; `tensions.boost_freshness_for_person(person_id, display_names)` symmetric to topic boost — wired into `compile_profile` re-sightings AND `detect_person_patterns` convergence; `compile_profile` + `compile_graph` GW-publish OPAQUE COUNTS only (no identities — "1 new person-sightings across 47 active"); `/cp/monitor` adds `Person correlation` Internal-category probe with disabled/ok/warn states tied to last-compile age. 16 new tests; full Q1-Q4.2.2 regression: **240 pass + 56 skip, 0 fail**.
- Person-correlation stack (2026-05-11; PROGRAM.md §42 + `crewai-team/docs/PERSON_CORRELATION.md`): four-level progressive-opt-in tracking of people who appear in the operator's inputs (gmail senders, calendar attendees, conversation participants — never ticket assignees, never body content). **All 15+ master switches default OFF**; each level requires the previous; L4 + L4.4 each gated by a typed-phrase confirmation (`ENABLE SOCIAL GRAPH` / `ENABLE GRAPH-DRIVEN SUGGESTIONS`). **L1 Presence** (`app/companion/person_model.py`) — per-modality counts + mute/forget/forget-all; **L2 Centrality** (`app/companion/person_centrality.py`) — operator-chosen formula from {`frequency`, `recency_weighted`, `cross_modal`}, lists sort by `last_seen` NEVER by score; **L3 Suggestions** (`app/companion/person_suggestions.py`) — dormancy + responsiveness nudges, all phrased as questions, ≤3/briefing cap; **L4 Social graph** (`app/companion/social_graph.py`) — co-appearance edges with 3-month half-life decay, per-pair mute, per-person opt-out-of-paths, full `forget-graph` wipe; **L4.1** shortest-path BFS (operator-initiated, opt-outs honored at intermediate position, every query logged); **L4.2** community detection (pure-Python label propagation + Newman's modularity, operator-dissolvable clusters); **L4.3** bridges + cut-vertices (iterative Tarjan's, arbiter gets capped boost ≤0.10); **L4.4** graph-driven suggestions (cluster_dormancy + bridge_maintenance + weak_tie_dormant; shares L3's 3-per-briefing cap). Goodhart guards: list sorts by `last_seen` not score, arbiter centrality boost capped 0.15, bridge boost capped 0.10, no body parsing, no LLM-generated nudges, operator-visible audit logs (`person_suggestions_emitted.jsonl` + `social_graph_query_log.jsonl`). DR exclusion via single `social_graph` substring fragment catches all 7 derived files. React `/cp/settings` `PersonCorrelationCard` with progressive disclosure + both typed-phrase inputs; three `/cp/companion` sub-tabs (People / Graph / Suggestions); Signal `/person` slash command with 9 subcommands. Composes with arbiter (5 salience inputs now), daily briefing (2 new sections), companion.loop (3 new LIGHT idle jobs). Observational and additive — zero behavior change when all switches OFF.
