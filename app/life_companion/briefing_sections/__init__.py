"""briefing_sections — candidate modules discovered by briefing_evolution.catalog.

Each module exposes ``ID``, ``DISPLAY_NAME``, ``DESCRIPTION``, ``gather()``.
Adding a file here is the only step to register a new candidate; the
catalog scans this package on first use.
"""
